/* Responsible for the logic when the user clocks on the workout card and that 
pops up it allows them to seal the exercises input, put the weights and have all
that information...all of that logic will be contained in this reusable workout card
*/

export default function WorkoutCard(){
    return(
         <></>     //These are our react fragments
    )
}